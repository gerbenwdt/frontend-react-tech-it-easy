// Helaas krijg ik deze functie niet werkend
export function televisionBrandList(inventory) {
    // return (
    //     <ul>
    //         {inventory.map((product) => {
    //             return <li key={product.type}>{product.brand}</li>
    //         })}
    //     </ul>
    // )
}
